# Dev Dashboard — Implementation Roadmap & Status

Resume point for a fresh session. The dashboard is a local-first single Go binary that embeds
a React SPA to inspect Dapr apps/workflows/sidecars. Authoritative design is the spec; this
file tracks **status, plan sequencing, and working conventions** that aren't derivable from code.

**Last updated:** 2026-06-27 · Plan 4 merged

## Source documents
- **Spec:** `docs/superpowers/specs/2026-06-25-dev-dashboard-design.md` (full design; plans 3–6 are derivable from it).
- **Plans:** `docs/superpowers/plans/2026-06-25-dashboard-NN-*.md`
- **Follow-ups:** `docs/superpowers/notes/plan-1-followups.md`, `plan-2-followups.md`, `plan-3-followups.md`, `plan-4-followups.md` (deferred Minor items).

## Plan status (6-plan roadmap)
| # | Plan | Status | Source spec sections |
|---|------|--------|----------------------|
| 1 | Foundation (Go/chi server, embedded React shell, routing, theme/density, small-screen guard, health/version, CLI, CI) | ✅ written + implemented + merged | §2, §3, §8, §9 |
| 2 | Discovery + Applications (`pkg/discovery`, `/api/apps`, global autorefresh, Applications list, App detail) | ✅ written + implemented + merged | §4, §5, §6.1–6.2, §9.1–9.4 |
| 3 | Workflows + state store + Terminate/Purge (`pkg/statestore`, `pkg/workflow`, live detail, hybrid removal, multi-store picker) | ✅ written + implemented + merged | §6.3, §7, §8 (workflows + terminate/purge), §9.7 status colors |
| 4 | Resources / Actors / Subscriptions (`pkg/resources`, metadata-derived Actors/Subscriptions, Components/Configurations + YAML viewer, cross-nav, lean nav icons) | ✅ written + implemented + merged | §6.4–6.6, §9.1 cross-nav, Icons row in §2 |
| 5 | Logs (SSE) + News/Resources sidebar | ⏳ not started | §6.7, §9.6 |
| 6 | Packaging (GoReleaser, install scripts, README incl. base-path note) | ⏳ not started | §2 Distribution |

**Next action:** write Plan 5 (Logs via SSE + News/Resources sidebar — grounded in the real Plans
1–4 code), then implement it. Each plan is its own write → implement → review → merge cycle.

## Working conventions (how Plans 1–2 were built — keep consistent)
- **Module path:** `github.com/diagridio/dev-dashboard`. **Go toolchain:** 1.26.x (go.mod). **Node:** 20 (build-time only).
- **Plan authoring:** superpowers `writing-plans` — bite-sized TDD tasks with full code, Global Constraints block copied from the spec, self-review.
- **Execution:** superpowers `subagent-driven-development` — one git worktree per plan off `main` (`git worktree add .claude/worktrees/<name> -b feat/<name>`), fresh implementer + reviewer subagent per task, fix subagents for Critical/Important, whole-branch review at the end, then `finishing-a-development-branch` (merge to `main` + push). Progress tracked in a `.superpowers/sdd/progress.md` ledger inside the worktree (gitignored).
- **Tests:** Go `testing` + `testify`, build tag `//go:build unit`; run `go test -tags unit ./...`. Frontend Vitest + Testing Library + MSW; `data-cy` selectors. **Vitest does NOT typecheck — also run `cd web && npm run build` (tsc+vite)** since CI's web job does.
- **CI gates:** `gofmt -l` (must be empty), `go vet -tags unit ./...`, `gotestsum -race`, web build+test. Run `gofmt -w` before committing Go.
- **Commit hygiene:** never `git add web/dist/` (only `web/dist/index.html` is a tracked static placeholder; built assets are gitignored and regenerated by `make build`). Commit messages end with the Co-Authored-By trailer.
- **State-store testing (Plan 3):** reuse cloudgrid conventions — `miniredis` (Redis), real temp-file SQLite, `testcontainers-go`/`go-sqlmock` (Postgres), behind `//go:build integration`.

## Key code landmarks (as of Plan 4)
- `pkg/discovery/`: `Metadata`/`Instance` now also carry `Actors []ActorType{Type,Count}`, `Subscriptions []Subscription{PubsubName,Topic,Rules []SubRule{Match,Path},DeadLetterTopic,Type}`, `Components []Component{Name,Type,Version}`, `EnabledFeatures []string`, `Placement string` — parsed from `/v1.0/metadata` (note input key `pubsubname` lowercase) and copied in `enrich` on the metadata-OK branch.
- `pkg/resources/`: `Service{List(ctx,Kind),Get(ctx,Kind,name)}` + `New(paths)`; `Kind`/`KindComponent`/`KindConfiguration`; `Resource{Name,Kind,Type,Version,Path,Raw,LoadedBy}`; walks `*.yaml` (`kind: Component|Configuration`) via `sigs.k8s.io/yaml`, dedup `filepath.Abs`; `List` Raw empty / `Get` reads Raw; `ErrNotFound`.
- `pkg/server/`: `apiRouter(v, apps, backend, stores, res)` adds `/api/{actors,subscriptions,resources}`. `actorsRouter`/`subscriptionsRouter(apps)` flatten across apps with `?appId=` filter (`ActorRow`/`SubscriptionRow`); `resourcesRouter(res, apps)` = `GET /?kind=` (400 on bad kind; `LoadedBy` for components via `loadedByIndex`) + `GET /{kind}/{name}` (404 ErrNotFound). `Options.Resources`.
- `cmd/root.go`: builds `resources.New(resPaths)` (`~/.dapr/components` + `~/.dapr` + app `ResourcePaths` + `ConfigPath` dirs) → `Options.Resources`.
- `web/src/`: `types/resources.ts` (`Actor`/`Subscription`/`ResourceSummary`/`ResourceDetail`); `hooks/useResources.ts` (`useActors`/`useSubscriptions` poll+`?appId`, `useResources`/`useResource` staleTime); `lib/yaml-highlight.tsx` (`highlightYaml`, hand-rolled, zero-dep); `pages/Actors.tsx` + `Subscriptions.tsx` (app-link, `?appId` URL filter), `pages/ResourceList.tsx` (`kind` prop → Components/Configurations), `pages/ResourceDetail.tsx` (YAML viewer + loaded-by); `AppDetail.tsx` Metadata section (enabled features + component chips → `/resources/component/{name}`); `components/icons/Icon.tsx` (local SVG, 7 nav glyphs) wired into `TopNav`. **No new runtime deps.**

## Key code landmarks (as of Plan 3)
- `pkg/statestore/`: `New(ctx, Component) (Store, error)` builds a components-contrib `state.Store` (redis/sqlite/postgresql) + `.Init`; `Store{Keys,Get,BulkGet,Delete,Set,Close}` over the public `state.KeysLiker`; `Detect(paths)` finds `state.*` component YAML; key helpers `WorkflowActorType`/`InstanceMetaPattern`/`InstancePrefix`/`InstanceKeyPattern`/`ParseInstanceID` (`<appId>||dapr.internal.<ns>.<appId>.workflow||<id>||<suffix>`); `SeedForTest` (integration only). Integration tests `//go:build integration` (miniredis + temp-file sqlite).
- `pkg/workflow/`: `Service{List,Get}` + `New(store, namespace, appIDs)`; `DecodeExecution(appID,id,history,customStatus)` via `durabletask-go/backend/runtimestate` over `protos.HistoryEvent`; `NormalizeStatus` → 6 dashboard statuses; `SelectMechanism(status,healthy,force)` (terminal→Purge / running→Terminate+Purge / force|no-sidecar→Force); `Remover{Remove,RemoveMany}` (beta1 `/v1.0-beta1/workflows/dapr/{id}/{terminate,purge}` + force key-delete); `Execution`/`ExecutionSummary`/`HistoryEvent`/`ListResult`/`ListQuery` DTOs.
- `pkg/server/`: `NewRouter(Options{BasePath, DistFS, Version, Apps, Backend, Stores})`, `apiRouter(v, apps, backend, stores)` mounts `/api/{health,version,apps,workflows,statestores}`, `SPAHandler` (History fallback, asset-404). `WorkflowBackend.ServiceFor(store)` selects per-request store (`?store=`; ""→active, unknown→404); `workflowsRouter` = list/detail/terminate/purge/bulk; `StoreRegistry`/`StoreInfo`, `TargetResolver`, `WorkflowRemover`.
- `cmd/`: flags add `--statestore`/`--namespace`(default "default"); `storeBackend` (per-store services + degraded fallback) + `storeRegistry` + `targetResolver` in `cmd/workflow.go`; `WorkflowComponent="dapr"`.
- `web/src/`: `types/workflow.ts`, `hooks/useWorkflows.ts` (`useWorkflows`/`useWorkflow`/`useStateStores`, all thread `?store=`), `hooks/useWorkflowRemoval.ts`, `components/StatusPill.tsx` + `ConfirmRemoveDialog.tsx` (`mechanismFor` mirrors Go tiers), `lib/wallclock.ts` (`elapsed`), `pages/Workflows.tsx` (filter/search/store-picker URL state, paging, selection, bulk remove) + `WorkflowDetail.tsx` (wall-clock + history timeline keyed by `sequenceId`), `theme.css` `--wf-<status>-bg/fg` pairs.

### Pre-Plan-3 landmarks
- `pkg/server/`: `apiRouter` originally mounted `/api/{health,version,apps}`; `SPAHandler` (History fallback, asset-404).
- `pkg/discovery/`: `Service{List,Get}`, `New(Scanner, *http.Client)`, `StandaloneScanner()` (dapr/cli), `Instance` type, metadata/health/parse/infer.
- `cmd/root.go`: cobra serve; flags `--port`(9090)/`--base-path`/`--no-open`; wires `discovery.New(StandaloneScanner(), ...)`.
- `web/src/`: `lib/api.ts` (`fetchJSON`), `lib/query.tsx` (`QueryProvider`,`makeQueryClient`), `lib/refresh.tsx` (`useRefreshInterval`,`refetchMs`), `lib/prefs.ts` (theme/density), `router.tsx` (History routes, base-path), `components/TopNav.tsx` (7-view nav), `pages/Applications.tsx` + `AppDetail.tsx`, `styles/theme.css` (tokens incl. `--ok/--warn/--bad`, spacing).
